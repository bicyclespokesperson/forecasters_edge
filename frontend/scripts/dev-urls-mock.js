#!/usr/bin/env node

console.log(`
🚀 Starting Forecaster's Edge Frontend with Mock Weather...

📍 Available URLs (Ctrl+Click to open):
   🎭 Local + Mock:       http://localhost:1234?mock
   ☁️  Prod + Mock:        http://localhost:1234?backend=prod&mock
   🏠 Local (no mock):    http://localhost:1234?backend=local

💡 Mock weather mode is already enabled!
`);